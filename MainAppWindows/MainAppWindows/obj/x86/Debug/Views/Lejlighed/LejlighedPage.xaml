<Page
    x:Class="MainAppWindows.Views.LejlighedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:MainAppWindows.Models"
    mc:Ignorable="d">
    
    <Grid x:ConnectionId='1'
        x:Name="ContentArea">

        <Grid.RowDefinitions>
            <RowDefinition x:ConnectionId='2' x:Name="TitleRow" Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:ConnectionId='3'
            x:Name="TitlePage"
            x:Uid="Lejlighed_Title"
            Style="{StaticResource PageTitleStyle}" />

        <Grid 
            Grid.Row="1" 
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.-->

            <StackPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock
                        Text="Address:"
                        FontSize="20"
                        Margin="20,20,10,20"/>
                    <TextBlock Text="{Binding LejlighedData.Adresse}" Margin="0,20,20,20" FontSize="20"/>
                </StackPanel>
                

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="1*" MinWidth="20"/>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Vertical" Grid.Column="1" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Stoerelse (m2):" Margin="0,4,10,4"/>
                            <TextBlock Text="{Binding LejlighedData.Stoerelse}" Margin="0,4" Foreground="#FF2D77D1"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Antal rum:" Margin="0,4,10,4"/>
                            <TextBlock Text="{Binding LejlighedData.Rum_Antal}" Margin="0,4" Foreground="#FF2D77D1"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Maandelig leje:" Margin="0,4,10,4"/>
                            <TextBlock Text="{Binding LejlighedData.Maandelig_Leje}" Margin="0,4" Foreground="#FF2D77D1"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Grid.Column="3" HorizontalAlignment="Center">
                        <StackPanel.Resources>
                            <Style TargetType="Button">
                                <Setter Property="MinWidth" Value="150"/>
                                <Setter Property="Height" Value="34"/>
                                <Setter Property="Margin" Value="0,10"/>
                            </Style>
                        </StackPanel.Resources>
                        <Button Content="Status indberetning" Command="{Binding NavToStatusIndPageCommand}"/>
                        <Button Content=""/>
                    </StackPanel>
                </Grid>

                   
                <Line Stroke="Gray" X2="1" Stretch="Fill" VerticalAlignment="Bottom" Margin="0,10"/>

                <StackPanel>
                    <ListView x:ConnectionId='4' x:Name="listView" HorizontalAlignment="Center" VerticalAlignment="Center"
                              ItemsSource="{Binding LejlighedsStatusRaporter}"
                              SelectedIndex="{Binding SelectedListViewIndex, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate                                         >
                                <StackPanel x:ConnectionId='5' Margin="5,0,0,5" Orientation="Horizontal">
                                    <TextBlock x:ConnectionId='6'                          Margin="0,0,10,0"/>
                                    <TextBlock x:ConnectionId='7'                            Margin="0,0,10,0"/>
                                    <!--<TextBlock Text="{x:Bind FaldstammeID}" Foreground="#FF2D77D1"/>
                                    <TextBlock Text=","/>
                                    <TextBlock Text="{x:Bind DelID}" Foreground="#FF2D77D1" Margin="0,0,10,0"/>-->
                                    <TextBlock x:ConnectionId='8'                      Margin="0,0,10,0"/>
                                    <TextBlock x:ConnectionId='9'                        Margin="0,0,10,0"/>
                                    <TextBlock x:ConnectionId='10'                     />
                                    <Button/>
                                    <Button/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                </StackPanel>
            
            </StackPanel>

        </Grid>
        <!--  Adaptive triggers  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='11' x:Name="WindowStates">
                <VisualState x:ConnectionId='12' x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:ConnectionId='13' x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="TitlePage.Margin" Value="48,0,12,7"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>

