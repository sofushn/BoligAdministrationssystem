<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainAppWindows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MainAppWindows.Converter"
    x:Class="MainAppWindows.Views.StatusIndberetningPage"
    xmlns:ViewModel="using:MainAppWindows.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:StatusRaportViewModel/>
    </Page.DataContext>


    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="bool2vis"/>
    </Page.Resources>

    <Grid x:ConnectionId='1'
        x:Name="ContentArea">

        <Grid.RowDefinitions>
            <RowDefinition x:ConnectionId='2' x:Name="TitleRow" Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <TextBlock x:ConnectionId='3'
            x:Name="TitlePage"
            x:Uid="StatusInd_Title"
            Style="{StaticResource PageTitleStyle}" />

        <Grid 
            Grid.Row="1" 
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.-->

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ComboBox
                    ItemsSource="{Binding RaportTypes}"
                    SelectedValue="{Binding CurrentType, Mode=TwoWay}"                   
                    Header="Raport type:"
                    Width="160" Margin="0,10"/>

                <TextBlock
                    Visibility="{Binding IsFIDVisible, Converter={StaticResource bool2vis}}"
                    Text="FaldstammeID"
                    Margin="0,10,0,8"/>
                <StackPanel
                    Orientation="Horizontal"
                    Margin="0,0,0,10"
                    Visibility="{Binding IsFIDVisible, Converter={StaticResource bool2vis}}">
                    <TextBox Text="{Binding FaldstammeID, Mode=TwoWay}" PlaceholderText="ID"/>
                    <TextBox Text="{Binding FaldstammeDelID, Mode=TwoWay}" PlaceholderText="Del ID"/>
                </StackPanel>

                
                <TextBox
                    Text="{Binding VindueID}" PlaceholderText="ID" Header="Vindue ID:"
                    Margin="0,0,0,10"
                    Visibility="{Binding IsVIDVisible, Converter={StaticResource bool2vis}}"/>

                <ComboBox
                    SelectedItem="{Binding CurrentStatus, Mode=TwoWay}"
                    ItemsSource="{Binding StatusValues}"
                    Header="Status:"
                    Width="160" Margin="0,10"/>

                <TextBox
                    Text="{Binding CurrentNote, Mode=TwoWay}"
                    AcceptsReturn="True" 
                    TextWrapping="Wrap"
                    ScrollViewer.VerticalScrollBarVisibility="Auto" 
                    Header="Note:"
                    Height=" 150" Width="400"  Margin="0,10"/>

                <Button
                    Content="Opret status rapport"
                    Command="{Binding CreateStatusRaportCommand}"
                    HorizontalAlignment="Center" Width="200" Height="40" Margin="50,50,50,15"/>
            </StackPanel>

</Grid>

        <!--  Adaptive triggers  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='4' x:Name="WindowStates">
                <VisualState x:ConnectionId='5' x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:ConnectionId='6' x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="TitlePage.Margin" Value="48,0,12,7"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
    </Grid>
</Page>

