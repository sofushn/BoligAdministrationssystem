<Page x:ConnectionId='1'
    x:Class="MainAppWindows.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainAppWindows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:ConnectionId='2'
        x:Name="ContentArea">

        <Grid.RowDefinitions>
            <RowDefinition x:ConnectionId='3' x:Name="TitleRow" Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:ConnectionId='4'
            x:Name="TitlePage"
            x:Uid="Login_Title"
            Style="{StaticResource PageTitleStyle}"/>

        <Grid 
            Grid.Row="1" 
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.-->

            <StackPanel VerticalAlignment="Center" Width="400">
                <TextBox x:ConnectionId='5' x:Name="usernameInput"
                    PlaceholderText="Username"
                    Margin="0,0,0,20"
                                                                   />
                <TextBox x:ConnectionId='6' x:Name="passwordInput"
                    PlaceholderText="Password"
                    Margin="0,0,0,20"
                                                                   />
                <Button Width="400" Height="35" Margin="0,10,0,20"
                    Content="Login"
                    Command="{Binding LoginCommand}"/>
            </StackPanel>

        </Grid>
        <!--  Adaptive triggers  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='7' x:Name="WindowStates">
                <VisualState x:ConnectionId='8' x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:ConnectionId='9' x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="TitlePage.Margin" Value="48,0,12,7"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>

